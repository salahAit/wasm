# PDF Barcode Processor (Codeabar)

تطبيق سطح مكتب لمعالجة ملفات PDF، استخراج أرقام الباركود من أسماء الملفات، دمج الباركود في كل الصفحات، وحفظ السجلات في قاعدة بيانات.

## 🚀 المميزات
- **واجهة عصرية**: استخدام مكتبة CustomTkinter.
- **معالجة آلية**: قراءة المجلد، توليد باركود، دمج في PDF.
- **سجلات**: حفظ العمليات في قاعدة بيانات SQLite محلية.
- **دعم الملفات**: يدعم تسمية الملفات بنمط `file-XX-XX-XX-XX.pdf`.

## 🛠️ المتطلبات
- Python 3.8+
- نظام تشغيل Windows (للملف النهائي) أو Linux (للتطوير).

## 📦 التثبيت والإعداد (Ubuntu/Linux)

1. **إنشاء البيئة الوهمية**:
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   ```

2. **تثبيت المكتبات**:
   ```bash
   pip install -r requirements.txt
   ```

## 🖥️ تشغيل التطبيق
لتشغيل التطبيق في وضع التطوير:
```bash
python3 main.py
```

## 🏗️ بناء الملف التنفيذي (.exe)

لتحويل السكربت إلى ملف تنفيذي يعمل على Windows:

### الخيار 1: استخدام سكربت البناء (الأسهل)
لقد قمت بإعداد سكربت `build.py` يقوم اكتشاف المسارات تلقائياً وتشغيل PyInstaller بالإعدادات الصحيحة.

1. تأكد من أنك على النظام الذي تريد بناء التطبيق له (لبناء .exe، يجب أن تكون على Windows أو تستخدم Wine/VM).
2. شغل الأمر:
   ```bash
   python build.py
   ```
3. ستجد الملف الناتج في مجلد `dist/Codeabar.exe`.

### الخيار 2: الأمر اليدوي (PyInstaller)
إذا كنت تفضل استخدام الأمر مباشرة:
```bash
pyinstaller --noconfirm --onefile --windowed --name "Codeabar" --add-data "<path_to_customtkinter>:customtkinter" main.py
```
*(ملاحظة: استبدل `<path_to_customtkinter>` بالمسار الصحيح للمكتبة على جهازك، واستخدم `;` بدلاً من `:` إذا كنت على Windows).*

## ⚠️ ملاحظة هامة لمستخدمي Linux (Cross-Compilation)
لا يمكن لـ PyInstaller إنشاء ملف `.exe` للويندوز مباشرة من داخل Linux. الحلول المقترحة:
1. **Virtual Machine**: نقل الكود إلى جهاز Windows أو Virtual Machine (مثل VirtualBox) وتشغيل `build.py` هناك.
2. **Wine**: تثبيت Python و PyInstaller داخل Wine وتشغيل البناء.
3. **GitHub Actions**: استخدام CI/CD لبناء المشروع تلقائياً عند رفعه.

## 🧪 التحقق والاختبار
تم توفير سكربت للتحقق من صحة المنطق (بدون الواجهة الرسومية):
```bash
python3 verify.py
```
سيقوم هذا السكربت بإنشاء بيئة اختبار `test_env`، توليد ملفات PDF، والتحقق من عملية الدمج واستخراج الباركود.

## 📂 هيكلة المشروع
- `main.py`: ملف التشغيل الرئيسي والواجهة الرسومية.
- `logic.py`: العمليات المنطقية (فصل الكود عن الواجهة).
- `verify.py`: سكربت الاختبار والتحقق.
- `verify_real_files.py`: سكربت للتحقق من المجلد الحقيقي للملفات.
- `build.py`: سكربت لتسهيل عملية البناء.
- `requirements.txt`: قائمة المكتبات المطلوبة.
